<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <WindowsSdkPackageVersion>10.0.26100.84</WindowsSdkPackageVersion>
    <Nullable>enable</Nullable>
    <UseWPF>true</UseWPF>
    <UseWindowsForms>true</UseWindowsForms>
    <AssemblyName>ConditioningControlPanel</AssemblyName>
    <RootNamespace>ConditioningControlPanel</RootNamespace>
    <Version>6.9.0</Version>
    <Company>CodeBambi</Company>
    <Product>Conditioning Control Panel</Product>
    <Description>A professional visual conditioning application with gamification features.</Description>
    <Copyright>Copyright © 2024-2026 CodeBambi</Copyright>
    <ApplicationIcon>Resources\app.ico</ApplicationIcon>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <PublishSingleFile>true</PublishSingleFile>
    <!-- Self-contained packaging is a PUBLISH-only concern. A plain "dotnet build" of the .sln
         pulls in the non-self-contained Tests project, and a non-self-contained executable
         cannot reference a self-contained one (NETSDK1151). Default off for build/run; the
         shipping build turns it on via -p:CcpPublish=true (build-installer.bat). A publish
         invocation that passes an explicit self-contained CLI flag still overrides this. -->
    <CcpPublish Condition="'$(CcpPublish)' == ''">false</CcpPublish>
    <SelfContained>$(CcpPublish)</SelfContained>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
    
    <!-- Disable most auto-generated assembly info (we have AssemblyInfo.cs) but enable version -->
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <GenerateAssemblyVersionAttribute>true</GenerateAssemblyVersionAttribute>
    <GenerateAssemblyFileVersionAttribute>true</GenerateAssemblyFileVersionAttribute>
    <GenerateAssemblyInformationalVersionAttribute>true</GenerateAssemblyInformationalVersionAttribute>

    <!-- Custom Main entry point (originally added for Velopack; kept for control over startup) -->
    <StartupObject>ConditioningControlPanel.App</StartupObject>

    <!-- Disable WPF auto-generated entry point so we can use our custom Main -->
    <EnableDefaultApplicationDefinition>false</EnableDefaultApplicationDefinition>
    
    <!-- Trimming disabled - not supported with WPF -->
    <PublishTrimmed>false</PublishTrimmed>
    
    <!-- Ready to Run -->
    <PublishReadyToRun>true</PublishReadyToRun>
    
    <!-- Deterministic builds -->
    <Deterministic>true</Deterministic>

    <!-- Suppress CA1416 platform compatibility warnings — this is a Windows-only WPF app -->
    <NoWarn>$(NoWarn);CA1416</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!-- UI Framework -->
    <!-- Buttplug 5.x is one package: Core + Client + WebSocket connector (the separate
         Buttplug.Client.Connectors.WebsocketConnector package is 3.x-only and is gone).
         5.0.1 speaks Buttplug message spec v4 (OutputCmd/InputCmd). -->
    <PackageReference Include="Buttplug" Version="5.0.1" />
    <PackageReference Include="DiscordRichPresence" Version="1.6.1.70" />
    <PackageReference Include="LibVLCSharp.WPF" Version="3.8.5" />
    <!-- Pin WindowsAppSDK (transitive from LibVLCSharp) and exclude its win10-* runtime assets
         which cause NETSDK1083 on .NET 8.0.400+ SDK with Windows 10 TFM -->
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.1.1" ExcludeAssets="all" PrivateAssets="all" />
    <PackageReference Include="MahApps.Metro" Version="2.4.10" />
    <PackageReference Include="MahApps.Metro.IconPacks" Version="5.0.0" />
    <!-- GPU-friendly 2D renderer for the new Chaos FX particle/glow layer (ChaosSkiaFxOverlay).
         CPU SKElement path is used for now (reliable on AllowsTransparency layered windows);
         native libSkiaSharp extracts via IncludeNativeLibrariesForSelfExtract for single-file. -->
    <PackageReference Include="SkiaSharp.Views.WPF" Version="2.88.8" />
    
    <!-- MVVM Framework -->
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.2.2" />
    
    <!-- Audio Control -->
    <PackageReference Include="NAudio" Version="2.2.1" />
    <PackageReference Include="NAudio.Wasapi" Version="2.2.1" />

    <!-- Offline speech recognition (Takeover "repeat after me" mechanic).
         Vosk = local, closed-grammar STT; native libvosk ships in the package's
         runtimes/win-x64 and extracts via IncludeNativeLibrariesForSelfExtract for
         single-file. The acoustic MODEL is NOT in the package — it's dropped into
         Resources\Models\vosk\ (see Content include below) and resolved at runtime by
         Services/Speech/SpeechService.cs. No model on disk => SpeechService no-ops.
         Privacy contract: audio stays on device, never transmitted, never written to disk. -->
    <PackageReference Include="Vosk" Version="0.3.38" />

    <!-- Dedicated offline wake-word engine for "Hey Bambi" (sherpa-onnx keyword spotting, Apache-2.0,
         fully local, NO API key). Vosk STT can't reliably spot a custom out-of-vocabulary name (its
         grammar ctor throws on the OOV token and it falls back to the free recognizer, mis-hearing the
         name ~half the time). sherpa-onnx KWS is an open-vocabulary spotter: the keyword is given as a
         subword-token sequence in keywords.txt — no retraining, no dictionary limit. It runs ONLY the
         wake stage; post-wake command/mantra capture stays on Vosk. The KWS MODEL is a drop-in under
         Resources\Models\sherpa-kws\ (see that folder's README); absent => the wake loop transparently
         falls back to the Vosk wake path, so nothing breaks if it isn't installed. The .runtime.win-x64
         dep carries the native lib. Privacy: identical to Vosk — audio stays on device, never sent. -->
    <PackageReference Include="org.k2fsa.sherpa.onnx" Version="1.13.3" />
    <PackageReference Include="org.k2fsa.sherpa.onnx.runtime.win-x64" Version="1.13.3" />
    <PackageReference Include="OllamaSharp" Version="5.4.16" />
    
    <!-- Logging -->
    <PackageReference Include="Serilog" Version="3.1.1" />
    <PackageReference Include="Serilog.Sinks.File" Version="5.0.0" />
    <PackageReference Include="Serilog.Sinks.Console" Version="5.0.0" />
    
    <!-- JSON Settings -->
    <!-- 13.0.4 is the floor required by Buttplug 5.0.1 (NU1605 otherwise). Patch bump only. -->
    <PackageReference Include="Newtonsoft.Json" Version="13.0.4" />
    
    <!-- Security / Encryption -->
    <PackageReference Include="System.Security.Cryptography.ProtectedData" Version="8.0.0" />
    
    <!-- System Tray -->
    <PackageReference Include="Hardcodet.NotifyIcon.Wpf" Version="1.1.0" />
    
    <!-- Embedded Browser -->
    <PackageReference Include="Microsoft.Web.WebView2" Version="1.0.2535.41" />

    <!-- AI Service -->
    <PackageReference Include="OpenAI-DotNet" Version="8.6.2" />
    
    <!-- SharpDX for Desktop Duplication API (DXGI) -->
    <PackageReference Include="SharpDX" Version="4.2.0" />
    <PackageReference Include="SharpDX.DXGI" Version="4.2.0" />
    <PackageReference Include="SharpDX.Direct3D11" Version="4.2.0" />
    <PackageReference Include="VideoLAN.LibVLC.Windows" Version="3.0.21">
      <IncludeAssets>all</IncludeAssets>
    </PackageReference>

    <!-- GIF Animation Support (also used for Circe avatar emotes — AvatarTubeWindow.CirceEmotes.cs) -->
    <PackageReference Include="XamlAnimatedGif" Version="2.3.0" />

    <!-- QR Code Generation (used by Remote Control tab for mobile pairing) -->
    <PackageReference Include="QRCoder" Version="1.6.0" />

    <!-- Webcam Tracking (Lab) — local-only face/eye detection.
         OpenCvSharp: video capture + image manipulation.
         OnnxRuntime: BlazeFace + FaceMesh + Iris ONNX models, CPU-only x64.
         Privacy contract: frames stay on device, never transmitted, never written to disk.
         See Services/WebcamTrackingService.cs. -->
    <PackageReference Include="OpenCvSharp4" Version="4.9.0.20240103" />
    <PackageReference Include="OpenCvSharp4.runtime.win" Version="4.9.0.20240103" />
    <PackageReference Include="Microsoft.ML.OnnxRuntime" Version="1.20.1" />

    <!-- WebRTC data channel for the Goon Game 1v1 transport (Services\GoonGame\).
         Pure managed .NET (BouncyCastle DTLS + its own SCTP stack), MIT, no native libs — so
         it survives the single-file / self-contained publish with no extraction shim.
         DATA CHANNEL ONLY: no media tracks are ever created; mic/cam never cross the wire.
         Verified at coding time on net8.0 + Windows 11 (26200): two in-process peers reach
         `connected` in ~0.85 s, an ordered/reliable channel opens at ~0.97 s, a 60 KB string
         round-trips intact (SCTP fragmentation works), and public STUN yields an srflx
         candidate. See docs/GOON_GAME_PLAN.md "Transport decisions". -->
    <PackageReference Include="SIPSorcery" Version="10.0.14" />

    <!-- SVG rendering for color emoji (Twemoji) — WPF can't render Segoe UI Emoji
         in color, so we ship the Twemoji SVG set and render via SharpVectors.
         See Helpers/EmojiImage.cs. -->
    <PackageReference Include="SharpVectors" Version="1.8.4.2" />
  </ItemGroup>

  <ItemGroup>
    <!-- Localization language files - must be on disk for File.ReadAllText() -->
    <Content Include="Localization\Languages\*.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
  </ItemGroup>

  <ItemGroup>
    <Content Include="appsettings.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
  </ItemGroup>

  <ItemGroup>
    <!-- Session files -->
    <Content Include="assets\sessions\*.session.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- Shipped AI personality presets (Strict Domme etc.) - the Companion tab's per-companion
         personality picker offers these; without this block they exist only in the repo and no
         user ever received them -->
    <Content Include="assets\prompts\*.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- AI knowledge base placeholder (read by Services/AIService/Enrichment/KnowledgeService) -->
    <Content Include="assets\knowledge.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- Chaos art (bubble sprites etc.), resolved at runtime by Services/Chaos/ChaosArt.cs
         from AppContext.BaseDirectory\Assets\Chaos\{kind}\{id}.png. Staging candidates are
         excluded so only the promoted finals ship. Remove from the default None glob first
         so the no-copy None item doesn't suppress this Content copy. -->
    <None Remove="assets\Chaos\**\*.png;assets\Chaos\**\*.gif;assets\Chaos\**\*.json" />
    <Content Include="assets\Chaos\**\*.png;assets\Chaos\**\*.gif;assets\Chaos\**\*.json" Exclude="assets\Chaos\**\_staging*\**">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- Profile cosmetics: the wardrobe registry + the 512x512 avatar-decoration and
         charm PNGs (Resources\cosmetics\<mod>\<id>.png). Loaded OFF DISK from
         AppContext.BaseDirectory like the Chaos art, NOT via pack:// - so a missing
         file degrades to "no decoration" instead of failing a Resource URI at runtime.
         Remove from the default None glob first so the no-copy None item doesn't
         suppress this Content copy. -->
    <None Remove="Resources\cosmetics\**\*.png;Resources\cosmetics\*.json" />
    <!-- The art pipeline leaves downscale sanity checks (<id>_104.png, <id>_28.png) and
         placement guides (<id>_guide.png) beside the finals. Only ids the registry names
         are ever loaded, so those would ship as dead weight - keep them out of the build. -->
    <Content Include="Resources\cosmetics\**\*.png;Resources\cosmetics\*.json"
             Exclude="Resources\cosmetics\**\*_28.png;Resources\cosmetics\**\*_104.png;Resources\cosmetics\**\*_guide.png">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- The Exclusives vault backdrop used to live at assets\exclusives\ as loose Content and
         was loaded off AppContext.BaseDirectory - outside Resources\, so no .ccpmod could ever
         shadow it. It moved to Resources\exclusives\ (declared as <Resource> further down) and
         now resolves through ModResourceResolver like every other mod-skinnable image. -->
  </ItemGroup>

  <!-- WebView2Loader.dll must be on disk - the WebView2 SDK loads it outside .NET's single-file extraction -->
  <Target Name="IncludeWebView2LoaderInPublish" AfterTargets="ComputeFilesToPublish">
    <ItemGroup>
      <ResolvedFileToPublish Include="$(NuGetPackageRoot)microsoft.web.webview2\1.0.2535.41\runtimes\win-x64\native\WebView2Loader.dll">
        <RelativePath>WebView2Loader.dll</RelativePath>
        <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
      </ResolvedFileToPublish>
    </ItemGroup>
  </Target>


  <ItemGroup>
    <!-- Rounded display font (Fredoka, OFL) for the Chaos multiplier hero number. Embedded as a
         Resource so the pack:// font URI (/Fonts/#Fredoka) resolves in the single-file build. -->
    <Resource Include="Fonts\Fredoka.ttf" />
  </ItemGroup>

  <ItemGroup>
    <!-- App icon -->
    <Resource Include="Resources\app.ico" />
    <Resource Include="Resources\february.png" />
    <Resource Include="Resources\february_drone.png" />
    <Resource Include="Resources\logo.png" />
    <Resource Include="Resources\logo2.png" />
    <!-- Weekly Intake Pass card art, one per niche (see Services\Quiz\IntakeNiche.cs).
         These must stay listed explicitly: there is no Resources\**\*.png glob, and a missing
         entry makes the pack:// URI fail at RUNTIME while the build still succeeds. -->
    <Resource Include="Resources\intake\pass_card_bambi.png" />
    <Resource Include="Resources\intake\pass_card_sissy.png" />
    <Resource Include="Resources\intake\pass_card_drone.png" />
    <Resource Include="Resources\intake\pass_card_circe.png" />
    <Resource Include="Resources\intake\pass_card_infection.png" />
    <!-- THE VAT's faucet (planning/one-descent/mockups/faucet.png, chroma-cut to
         alpha, spout tip at 90.6% / 98.9%). Same rule as the intake cards above:
         no glob, so a missing entry fails the pack:// URI at RUNTIME only. -->
    <Resource Include="Resources\descent\faucet.png" />
    <Resource Include="Resources\bubble.png" />
    <Resource Include="Resources\speechbubble1.png" />
    <Resource Include="Resources\speechbubble2.png" />
    <Resource Include="Resources\discord.png" />
    <Resource Include="Resources\lockdown_icon.png" />
    <!-- The Emergency Exit button face (Views\Tabs\LockdownTabView.xaml). No Resources\**\*.png
         glob exists, so this line is what keeps the pack:// URI resolvable; without it the
         Image simply renders nothing and the XAML-drawn slab underneath carries the button. -->
    <Resource Include="Resources\lockdown\emergency_exit.png" />
    <Resource Include="Resources\spiral.gif" />
    <!-- Corner-overlay spiral: the same loop pre-scaled to 360x240. The corner GIF draws at
         70-300px and used to fall back to the 2400x1600 spiral above, which the render thread then
         resampled every frame on a layered window - the program-day freeze. -->
    <Resource Include="Resources\spiral_corner.gif" />
    <Resource Include="Resources\Patreon tier1.png" />
    <Resource Include="Resources\Patreon tier2.png" />
    <Resource Include="Resources\Patreon tier3.png" />
    <Resource Include="Resources\Pink filter.webp" />
    <Resource Include="Resources\prime subject.webp" />

    <!-- Content Pack preview images -->
    <Resource Include="Resources\pack1.png" />
    <Resource Include="Resources\pack2.png" />
    
    <!-- Avatar Tube images - MUST be Resource for pack:// URIs to work -->
    <Resource Include="Resources\tube.png" />
    <Resource Include="Resources\tube2.png" />
    <!-- TUBE GLASS: MIDNIGHT (Prize Counter restock). Conditioned on Exists() because the art
         lands separately from the wiring: without the condition a build made before the PNGs are
         dropped in fails outright, and with it the same build simply falls back to the standard
         pane (AvatarTubeWindow.SetTubeStyle resolves null -> standard). Drop the two files in and
         the next build picks them up with no csproj edit. -->
    <Resource Include="Resources\tube_midnight.png" Condition="Exists('Resources\tube_midnight.png')" />
    <Resource Include="Resources\tube2_midnight.png" Condition="Exists('Resources\tube2_midnight.png')" />
    
    <!-- Avatar Set 1: Default (Level 1-19) -->
    <Resource Include="Resources\avatar_pose1.png" />
    <Resource Include="Resources\avatar_pose2.png" />
    <Resource Include="Resources\avatar_pose3.png" />
    <Resource Include="Resources\avatar_pose4.png" />
    
    <!-- Avatar Set 2: Level 20+ (Neon/Circuit style) -->
    <Resource Include="Resources\avatar2_pose1.png" />
    <Resource Include="Resources\avatar2_pose2.png" />
    <Resource Include="Resources\avatar2_pose3.png" />
    <Resource Include="Resources\avatar2_pose4.png" />
    
    <!-- Avatar Set 3: Level 50+ (Marionette/Puppet style) -->
    <Resource Include="Resources\avatar3_pose1.png" />
    <Resource Include="Resources\avatar3_pose2.png" />
    <Resource Include="Resources\avatar3_pose3.png" />
    <Resource Include="Resources\avatar3_pose4.png" />
    
    <!-- Avatar Set 4: Level 50-124 (Sunglasses/Glamour style) -->
    <Resource Include="Resources\avatar4_pose1.png" />
    <Resource Include="Resources\avatar4_pose2.png" />
    <Resource Include="Resources\avatar4_pose3.png" />
    <Resource Include="Resources\avatar4_pose4.png" />

    <!-- Avatar Set 5: Level 125-149 (Brainwashed Slavedoll) -->
    <Resource Include="Resources\avatar5_pose1.png" />
    <Resource Include="Resources\avatar5_pose2.png" />
    <Resource Include="Resources\avatar5_pose3.png" />
    <Resource Include="Resources\avatar5_pose4.png" />

    <!-- Avatar Set 6: Level 150+ (Platinum Puppet) -->
    <Resource Include="Resources\avatar6_pose1.png" />
    <Resource Include="Resources\avatar6_pose2.png" />
    <Resource Include="Resources\avatar6_pose3.png" />
    <Resource Include="Resources\avatar6_pose4.png" />

    <!-- Avatar Set 7: Level 75+ (Bambi Cow) -->
    <Resource Include="Resources\avatar7_pose1.png" />
    <Resource Include="Resources\avatar7_pose2.png" />
    <Resource Include="Resources\avatar7_pose3.png" />
    <Resource Include="Resources\avatar7_pose4.png" />

    <!-- Animated Avatars - uncomment when GIF files are added to Resources
    <Resource Include="Resources\animated1_1.gif" />
    <Resource Include="Resources\animated2_1.gif" />
    <Resource Include="Resources\animated3_1.gif" />
    <Resource Include="Resources\animated4_1.gif" />
    -->

    <!-- Animated companion emotes (transparent looping GIF, 15fps) + per-set mapping.
         Rendered via XamlAnimatedGif, wired in AvatarTubeWindow.CirceEmotes.cs (registry-driven).
         To add a pose/mod: drop Resources\<folder>\ (clips + emotes.json), add a line to the
         registry below, and add a <Resource> include here. See tools/avatar-emotes/README.md.
         Embedded as Resource so pack:// URIs resolve in the single-file build (~30 MB / pose). -->
    <Resource Include="Resources\avatar_emotes_registry.json" />
    <Resource Include="Resources\circe_emotes\*.gif" />
    <Resource Include="Resources\circe_emotes\emotes.json" />
    <!-- Circe level emote sets: avatar set 2 = L2 (The Claim/key), 3 = L3 (The Pull/spiral), 4 = L4 (The Bind/chain). -->
    <Resource Include="Resources\circe_emotes_p2\*.gif" />
    <Resource Include="Resources\circe_emotes_p2\emotes.json" Condition="Exists('Resources\circe_emotes_p2\emotes.json')" />
    <Resource Include="Resources\circe_emotes_p3\*.gif" />
    <Resource Include="Resources\circe_emotes_p3\emotes.json" Condition="Exists('Resources\circe_emotes_p3\emotes.json')" />
    <Resource Include="Resources\circe_emotes_p4\*.gif" />
    <Resource Include="Resources\circe_emotes_p4\emotes.json" Condition="Exists('Resources\circe_emotes_p4\emotes.json')" />
    <!-- Avatar-0 (blonde) emote set: shared by BambiSleep + Sissy at avatar set 1. -->
    <Resource Include="Resources\avatar0_emotes\*.gif" />
    <Resource Include="Resources\avatar0_emotes\emotes.json" Condition="Exists('Resources\avatar0_emotes\emotes.json')" />
    <Resource Include="Resources\avatar0_emotes\clip_timing.json" Condition="Exists('Resources\avatar0_emotes\clip_timing.json')" />

    <!-- Achievement images -->
    <Resource Include="Resources\achievements\*.png" />

    <!-- Skill tree images (individual skill files) -->
    <Resource Include="Resources\skills\*.png" />

    <!-- Twemoji SVG set for color-emoji rendering across the UI.
         ~4000 files, ~19 MB on disk, MIT/CC-BY 4.0. Source: github.com/jdecked/twemoji.
         Resolved at runtime via Helpers/EmojiImage.cs (codepoint -> pack:// URI). -->
    <Resource Include="Resources\Twemoji\*.svg" />
  </ItemGroup>

  <ItemGroup>
    <!-- Feature images - embedded as Resource for pack:// URIs to work -->
    <Resource Include="Resources\features\*.png" />
    <!-- Nav rail door art (2026-08-11 emoji purge): one emblem per door, referenced by
         MainWindow.xaml pack URIs - a missing file here crashes InitializeComponent. -->
    <Resource Include="Resources\nav\*.png" />

    <!-- Training Programs art: day mood plates and program sigils.
         SINGLE LEVEL on purpose - a Resources\programs\<sub>\ folder would compile fine and then
         fail at runtime with a broken pack:// URI, because nothing here globs recursively. -->
    <Resource Include="Resources\programs\*.png" />

    <!-- Velvet Vault room art. Resource, NOT Content: ModResourceResolver's last link is a
         pack:// URI, so a Content-only file resolves to nothing once the mod and event probes
         miss (that is exactly how this backdrop was unreachable to mods before). -->
    <Resource Include="Resources\exclusives\*.png" />

    <!-- Profile hero banners (CosmeticsCatalog.Banners). Resource, NOT Content: the banner
         loader is pack:// only, and these must never fall under the Resources\cosmetics\**
         Content glob that ships the wardrobe art loose on disk. -->
    <Resource Include="Resources\banners\*.png" />
  </ItemGroup>

  <ItemGroup>
    <!-- Blink Trainer stage demo assets — SFW abstract gradients shown on the
         dedicated Blink Trainer page when no live preview is running, and to
         non-premium users behind the gate. Regenerate via
         assets\BlinkTrainer\Demo\generate_demos.ps1. -->
    <Resource Include="assets\BlinkTrainer\Demo\*.png" />
  </ItemGroup>

  <ItemGroup>
    <!-- Haptics setup guide images -->
    <Resource Include="Resources\haptics_guide\*.png" />
  </ItemGroup>

  <ItemGroup>
    <!-- Session completion card images -->
    <Resource Include="Resources\Cards\*.png" />
  </ItemGroup>

  <ItemGroup>
    <!-- Bespoke per-quest art (one PNG per quest id), bundled in-app so quests
         render without any CDN dependency. Loaded via pack:// by quest id. -->
    <Resource Include="Resources\quests\*.png" />
  </ItemGroup>

  <!-- ==========================================================================================
       CONTENT PACKS — what NO LONGER ships in the installer.
       See docs/CONTENT_PACKS_PLAN.md §1 (exact table) and §6 (build changes).

       ~1.36 GB of version-stable audio (plus the two bundled .ccpmod archives, removed further
       down) leaves the publish tree and is instead zipped by Scripts\build-content-packs.ps1,
       hosted on the vX.Y.0 GitHub release, and downloaded at runtime into
       %LOCALAPPDATA%\ConditioningControlPanel\content\.

       These four properties are the single source of truth and are consumed in BOTH places the
       content is declared: the <Content> itemgroups below AND the CopyContentAfterPublish target
       at the bottom of this file. The *Abs variants are the same patterns made absolute, because
       that target's Include is absolute too — keep each pair mirrored. If you change anything
       here, change Scripts\build-content-packs.ps1 to match, or the files will exist in neither
       the installer nor a pack.

       Invariants:
         * ALL manifests stay in-box: every *.json (bark_rules, mantras, avatar_manifest,
           vo_manifest, sfx_manifest, vn\manifest.json) and DTRH barks\manifest.js.
           A missing manifest is not a graceful degrade — it's a hang or a crash.
         * Audio that STAYS (nothing below may match it): Resources\sounds root files,
           bubbles\, chaos\, mindwipe\, braindrain\, companion_audio root (egg_*),
           Resources\sub_audio\, Resources\AwarenessPresets\audio\.
         * builtin-sissyhypno moves its portraits (*.png) too — its whole payload except the
           three .json manifests (plan §1, "mod-sissy").
         * dtrh\assets\**\*.mp3 is a deliberate catch-all: every mp3 under it moves (per-persona
           barks, bubbles sfx/voices/drops, vn\vo, audio\drone1). The art (png/webp/gif/webm)
           and code (js/json) stay.

       Extension audit 2026-08-03 — the only extensions present in the moving folders are
       .mp3 (everywhere), .wav (Resources\sounds\flashes_audio only) and .png
       (builtin-sissyhypno\portraits only). The three builtin-* mod folders nevertheless list
       the full $AudioExt set (.mp3/.wav/.ogg/.m4a) that build-content-packs.ps1 packs from
       them, so a future .ogg there cannot ship in-box AND in a pack (double-ship: the in-box
       copy permanently shadows the downloaded one, because ContentLocator probes BaseDirectory
       first). installer.iss [InstallDelete] carries the same extension list.

       DRIFT IS ENFORCED, NOT TRUSTED: build-content-packs.ps1 hardcodes a mirror of the two
       Exclude properties below and hard-fails the pack build if the stripped set and the packed
       set disagree in either direction. Edit these properties → edit $CsprojStripPatterns in
       that script in the same commit, or the next pack build stops with a diff.
       ========================================================================================== -->
  <PropertyGroup>
    <ContentPackSoundsExclude>
      Resources\sounds\flashes_audio\**\*.mp3;
      Resources\sounds\flashes_audio\**\*.wav;
      Resources\sounds\companion_audio\mods\builtin-bambisleep\**\*.mp3;
      Resources\sounds\companion_audio\mods\builtin-bambisleep\**\*.wav;
      Resources\sounds\companion_audio\mods\builtin-bambisleep\**\*.ogg;
      Resources\sounds\companion_audio\mods\builtin-bambisleep\**\*.m4a;
      Resources\sounds\companion_audio\mods\builtin-locked\**\*.mp3;
      Resources\sounds\companion_audio\mods\builtin-locked\**\*.wav;
      Resources\sounds\companion_audio\mods\builtin-locked\**\*.ogg;
      Resources\sounds\companion_audio\mods\builtin-locked\**\*.m4a;
      Resources\sounds\companion_audio\mods\builtin-sissyhypno\**\*.mp3;
      Resources\sounds\companion_audio\mods\builtin-sissyhypno\**\*.wav;
      Resources\sounds\companion_audio\mods\builtin-sissyhypno\**\*.ogg;
      Resources\sounds\companion_audio\mods\builtin-sissyhypno\**\*.m4a;
      Resources\sounds\companion_audio\mods\builtin-sissyhypno\**\*.png
    </ContentPackSoundsExclude>
    <!-- NOTE: $(MSBuildProjectDirectory), not $(ProjectDir). ProjectDir is defined by
         Microsoft.Common.CurrentVersion.targets, which the SDK imports AFTER this file's body,
         so it evaluates to empty here (it only works inside a <Target>, like the one below). -->
    <ContentPackSoundsExcludeAbs>
      $(MSBuildProjectDirectory)\Resources\sounds\flashes_audio\**\*.mp3;
      $(MSBuildProjectDirectory)\Resources\sounds\flashes_audio\**\*.wav;
      $(MSBuildProjectDirectory)\Resources\sounds\companion_audio\mods\builtin-bambisleep\**\*.mp3;
      $(MSBuildProjectDirectory)\Resources\sounds\companion_audio\mods\builtin-bambisleep\**\*.wav;
      $(MSBuildProjectDirectory)\Resources\sounds\companion_audio\mods\builtin-bambisleep\**\*.ogg;
      $(MSBuildProjectDirectory)\Resources\sounds\companion_audio\mods\builtin-bambisleep\**\*.m4a;
      $(MSBuildProjectDirectory)\Resources\sounds\companion_audio\mods\builtin-locked\**\*.mp3;
      $(MSBuildProjectDirectory)\Resources\sounds\companion_audio\mods\builtin-locked\**\*.wav;
      $(MSBuildProjectDirectory)\Resources\sounds\companion_audio\mods\builtin-locked\**\*.ogg;
      $(MSBuildProjectDirectory)\Resources\sounds\companion_audio\mods\builtin-locked\**\*.m4a;
      $(MSBuildProjectDirectory)\Resources\sounds\companion_audio\mods\builtin-sissyhypno\**\*.mp3;
      $(MSBuildProjectDirectory)\Resources\sounds\companion_audio\mods\builtin-sissyhypno\**\*.wav;
      $(MSBuildProjectDirectory)\Resources\sounds\companion_audio\mods\builtin-sissyhypno\**\*.ogg;
      $(MSBuildProjectDirectory)\Resources\sounds\companion_audio\mods\builtin-sissyhypno\**\*.m4a;
      $(MSBuildProjectDirectory)\Resources\sounds\companion_audio\mods\builtin-sissyhypno\**\*.png
    </ContentPackSoundsExcludeAbs>
    <ContentPackWebExclude>
      Resources\web\intake\assets\vo\**\*.mp3;
      Resources\web\intake\assets\sfx\**\*.mp3;
      Resources\web\intake\assets\music\**\*.mp3;
      Resources\web\dtrh\assets\**\*.mp3
    </ContentPackWebExclude>
    <ContentPackWebExcludeAbs>
      $(MSBuildProjectDirectory)\Resources\web\intake\assets\vo\**\*.mp3;
      $(MSBuildProjectDirectory)\Resources\web\intake\assets\sfx\**\*.mp3;
      $(MSBuildProjectDirectory)\Resources\web\intake\assets\music\**\*.mp3;
      $(MSBuildProjectDirectory)\Resources\web\dtrh\assets\**\*.mp3
    </ContentPackWebExcludeAbs>
    <!-- The four arcademy *demo.html browser harnesses. Listed once here and referenced by
         BOTH the <Content> Exclude below AND the CopyContentAfterPublish WebFiles Exclude,
         which re-globs Resources\web independently - miss the second one and the
         harness pages come straight back into the publish tree (and so the installer). -->
    <ArcademyHarnessExclude>
      Resources\web\arcademy\annex\demo.html;
      Resources\web\arcademy\vn\demo.html;
      Resources\web\arcademy\emi\demo.html;
      Resources\web\arcademy\shell\recordsroom.demo.html
    </ArcademyHarnessExclude>
    <ArcademyHarnessExcludeAbs>
      $(MSBuildProjectDirectory)\Resources\web\arcademy\annex\demo.html;
      $(MSBuildProjectDirectory)\Resources\web\arcademy\vn\demo.html;
      $(MSBuildProjectDirectory)\Resources\web\arcademy\emi\demo.html;
      $(MSBuildProjectDirectory)\Resources\web\arcademy\shell\recordsroom.demo.html
    </ArcademyHarnessExcludeAbs>
  </PropertyGroup>

  <ItemGroup>
    <!-- Sound files - must be copied to output and publish for file system access.
         Audio listed in $(ContentPackSoundsExclude) ships as a downloadable pack instead. -->
    <Content Include="Resources\sounds\**\*" Exclude="$(ContentPackSoundsExclude)">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- Awareness v2 angle cards. Shipped as Content next to app_clusters.json by the glob above so a
         mod can REPLACE the file on disk, and ALSO embedded here so there is a fail-closed floor: a
         missing, oversized, malformed or hostile override falls back to this copy rather than to a
         prompt with no angle cards and no persona digest.
         See Services\Awareness\AwarenessAngleCards.cs. -->
    <EmbeddedResource Include="Resources\sounds\companion_audio\awareness_angles.json" />
    <!-- Endless three.js "rabbit hole" tunnel background (HTML/JS + vendored three.js) and the
         DtRH browser game (Resources\web\dtrh). Served to a WebView2 via virtual hosts, so they
         must live on disk in the output. Remove from the default None glob first so a no-copy
         None item can never suppress the Content copy for new extensions (woff2/json/etc).
         Audio listed in $(ContentPackWebExclude) ships as a downloadable pack instead.
         The four arcademy *demo.html pages are BROWSER HARNESSES - standalone proofs for the
         annex, the VN layer, EMI and the Records room, opened straight from a checkout during
         development. Nothing in the app or the page graph links them (only two comments name
         them), so they are dead weight in the installer and a stray unversioned entry point in
         the virtual host. Excluded, not deleted: they are how those layers get worked on. -->
    <None Remove="Resources\web\**\*" />
    <Content Include="Resources\web\**\*"
             Exclude="Resources\web\**\CLAUDE.md;Resources\web\**\CAPTCHA_*.md;Resources\web\**\BUILD_PLAN.md;
                      $(ArcademyHarnessExclude);
                      $(ContentPackWebExclude)">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- EMI desk lines (Resources\emi\desk-lines.json): read from disk at runtime by the desk
         line engine, so it ships next to the exe like the web assets above. -->
    <Content Include="Resources\emi\**\*.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- EMI desk fonts (Resources\emi\fonts\*.ttf): loaded through a FontFamily base URI at
         runtime, not as a WPF Resource, so they ship beside the exe exactly like the lines file. -->
    <Content Include="Resources\emi\**\*.ttf">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- Subliminal audio files -->
    <Content Include="Resources\sub_audio\**\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- Awareness Engine preset packs -->
    <Content Include="Resources\AwarenessPresets\**\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- Deeper demo enhancements (seeded into user's library on first launch) -->
    <Content Include="Resources\DeeperDemos\**\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- Webcam tracking ONNX models (offline-only, no network calls).
         README.md documents required files; .onnx binaries dropped in by build process. -->
    <Content Include="Resources\Models\**\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- Custom spirals folder -->
    <Content Include="Spirals\**\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
    <!-- Bundled tutorial video clips (short, low-res, muted loops) played by
         HelpVideoWindow. File-based access via LibVLC requires disk copy. -->
    <Content Include="Resources\tutorial_videos\**\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
    </Content>
  </ItemGroup>

  <!-- Bundled built-in mod packages — REMOVED FROM THE BUILD (docs/CONTENT_PACKS_PLAN.md §1/§6).
       DroneMod\drone-mode.ccpmod (184 MB) and LockedMod\locked-resources.ccpmod (125 MB) used to
       be <Content> here and were extracted by ModService on first launch into
       %LOCALAPPDATA%\ConditioningControlPanel\builtin_mods\<id>\. They now ship inside the
       `mod-drone` / `mod-locked` release packs at the in-zip path packs\<name>.ccpmod (see
       Scripts\build-content-packs.ps1), landing at
       %LOCALAPPDATA%\ConditioningControlPanel\content\packs\<name>.ccpmod, which the mod-install
       wave (plan §3, phase C) extracts through the bundled-builtin path. The source files stay in
       the repo under DroneMod\ / LockedMod\ — they are the pack build inputs, just not build
       output any more. installer.iss [InstallDelete] removes the stale copies from upgraders. -->

  <ItemGroup>
    <Folder Include="Themes\" />
  </ItemGroup>

  <!-- Ensure all content files are copied during publish -->
  <Target Name="CopyContentAfterPublish" AfterTargets="Publish">
    <ItemGroup>
      <!-- The excludes below MUST mirror the <Content> items above, or this target silently
           re-copies the stripped audio (and the arcademy harness pages) back into the publish
           tree. That is why both live in properties. See docs/CONTENT_PACKS_PLAN.md §6. -->
      <SoundFiles Include="$(ProjectDir)Resources\sounds\**\*" Exclude="$(ContentPackSoundsExcludeAbs)" />
      <WebFiles Include="$(ProjectDir)Resources\web\**\*"
                Exclude="$(ProjectDir)Resources\web\**\CLAUDE.md;$(ProjectDir)Resources\web\**\CAPTCHA_*.md;$(ProjectDir)Resources\web\**\BUILD_PLAN.md;
                         $(ContentPackWebExcludeAbs);
                         $(ArcademyHarnessExcludeAbs)" />
      <SubAudioFiles Include="$(ProjectDir)Resources\sub_audio\**\*" />
      <AwarenessPresetFiles Include="$(ProjectDir)Resources\AwarenessPresets\**\*" />
      <DeeperDemoFiles Include="$(ProjectDir)Resources\DeeperDemos\**\*" />
      <SpiralFiles Include="$(ProjectDir)Spirals\**\*" />
      <TutorialVideoFiles Include="$(ProjectDir)Resources\tutorial_videos\**\*" />
    </ItemGroup>
    <Copy SourceFiles="@(SoundFiles)" DestinationFolder="$(PublishDir)Resources\sounds\%(RecursiveDir)" SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(WebFiles)" DestinationFolder="$(PublishDir)Resources\web\%(RecursiveDir)" SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(SubAudioFiles)" DestinationFolder="$(PublishDir)Resources\sub_audio\%(RecursiveDir)" SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(AwarenessPresetFiles)" DestinationFolder="$(PublishDir)Resources\AwarenessPresets\%(RecursiveDir)" SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(DeeperDemoFiles)" DestinationFolder="$(PublishDir)Resources\DeeperDemos\%(RecursiveDir)" SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(SpiralFiles)" DestinationFolder="$(PublishDir)Spirals\%(RecursiveDir)" SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(TutorialVideoFiles)" DestinationFolder="$(PublishDir)Resources\tutorial_videos\%(RecursiveDir)" SkipUnchangedFiles="true" />
    <Message Importance="High" Text="Copied content files to publish folder" />
  </Target>

  <!-- Copy LibVLC native libraries after publish (required for video playback on Windows N/KN) -->
  <Target Name="CopyLibVLCAfterPublish" AfterTargets="Publish">
    <PropertyGroup>
      <LibVLCPath>$(NuGetPackageRoot)videolan.libvlc.windows\3.0.21\build\x64\</LibVLCPath>
    </PropertyGroup>
    <ItemGroup>
      <LibVLCFiles Include="$(LibVLCPath)**\*" />
    </ItemGroup>
    <Copy SourceFiles="@(LibVLCFiles)" DestinationFolder="$(PublishDir)libvlc\%(RecursiveDir)" SkipUnchangedFiles="true" />
    <Message Importance="High" Text="Copied LibVLC files from $(LibVLCPath) to $(PublishDir)libvlc\" />
  </Target>

</Project>
